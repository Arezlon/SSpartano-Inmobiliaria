@model SSpartanoInmobiliaria.Models.Pago

@{
    ViewData["Title"] = "Baja Pago";
}

<h1>Baja</h1>
<div>
    <h4>Propietario</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
    </dl>
    <hr />
    <h5>¿Está seguro de querer eliminar el pago?</h5>

    <form asp-action="Delete">
        <input type="submit" value="Eliminar" class="btn btn-danger" /> |
        <!--<a asp-route-ContratoId="@Model.ContratoId" asp-action="Index">Volver a la lista</a>-->
    </form>
</div>
